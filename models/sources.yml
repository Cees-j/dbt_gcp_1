  version: 2

sources:
  - name: raw_data
    description: "Raw data dumps from various systems"
    database: "{{ var('project_name') }}"
    schema: "dbt_cees_bq"
    tables:
      - name: raw_policies
        description: "Events generated by users"
      - name: raw_claims
        description: "Logs from the system"
      - name: raw_policyholders
        description: "raw policy holders upload"