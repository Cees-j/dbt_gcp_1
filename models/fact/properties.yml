version 2:

models:
  - name: fact_claim
    config:
      schema: fact
      materialized: view

    columns:
      - name: claim_status
        description: "claim status"
        tests: 
          - not_null
